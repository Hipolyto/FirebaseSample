using Java;
using Java.Lang;


using System.Runtime.Serialization;
using System;

namespace NotificationFirebase.Droid.Model
{
}
    /*
    [DataContract]
    public class ChatMessage : Java.Lang.Object, Android.Runtime.IJavaObject
    {
        [DataMember]
        public string id;
        //public String id;

        [DataMember]
        public string text;
        //public String text;

        [DataMember]
        public string name;
        //public String name;

        [DataMember]
        public string photoUrl;
        //public String photoUrl;

        [DataMember]
        public string imageUrl;
        //public String imageUrl;


        public ChatMessage()
        {
        }

        public ChatMessage(string text, string name, string photoUrl, string imageUrl)
        {
            if (!string.IsNullOrWhiteSpace(text))
                this.text = text;//= new String(text);
            if (!string.IsNullOrWhiteSpace(name))
                this.name = name;//= new String(name);
            if (!string.IsNullOrWhiteSpace(photoUrl))
                this.photoUrl = photoUrl; // = new String(photoUrl);
            if (!string.IsNullOrWhiteSpace(imageUrl))
                this.imageUrl = imageUrl;//= new String(imageUrl);
        }
        */