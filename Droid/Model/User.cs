using Java;
using Java.Lang;
using Java.Util;

namespace NotificationFirebase.Droid.Model
{
    /*
    public class User : Java.Lang.Object
    {
        public String uid;
        public String email;
        public String displayname;

        public String plataforma;
        public String deviceToken;
        public Double latitud;
        public Double longitud;


        public User()
        {
        }

        public User(String username, String email)
        {
            this.displayname = username;
            this.email = email;
        }

        public static User GetUserFrom(NotificationFirebase.Model.User userForms)
        {
            return new NotificationFirebase.Droid.Model.User
            {
                uid = new String(userForms.Uid),
                email = new String(userForms.Email),
                displayname = new String(userForms.Displayname),

                plataforma = new String(userForms.Plataforma),
                deviceToken = new String(userForms.DeviceToken),
                latitud = new Double(userForms.Latitud),
                longitud = new Double(userForms.Longitud)
            };
        }

        public static HashMap UserModelToMap(this User model)
        {
            HashMap map = new HashMap();
            map.Put("Uuid", model.uid);
            return map;
        }
    }*/
}
